{
  "name": "gpa-horas-extras",
  "version": "2.2.0",
  "private": true,
  "description": "Sistema de registro y autorización de horas extras — Grupo GPA Aqua",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Sitio estatico + funciones serverless: no requiere build'",
    "check": "node --check api/login.js && node --check api/registros.js && node --check api/autorizar.js && node --check api/monitor.js && node --check api/usuarios.js && node --check api/sabados.js && node --check api/reporte.js && node --check api/diagnostico.js && node --check api/limpiar.js && node --check lib/db.js && node --check lib/auth.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.5",
    "bcryptjs": "^2.4.3",
    "xlsx": "^0.18.5"
  }
}
